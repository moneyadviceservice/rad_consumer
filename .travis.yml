language: ruby
sudo: false
cache: bundler
dist: precise
rvm:
  - 2.2.2
services:
  - elasticsearch
before_script:
  - cp config/example.database.yml config/database.yml
  - psql -c "create database rad_test;" -U postgres
  - bundle exec rake db:migrate
  - npm install
  - npm install -g bower karma-cli
  - bundle exec bowndler update
script:
  - bundle exec rspec
  - npm test
  - bundle exec rubocop -DS
env:
  - MAS_CMS_URL=http://localhost:3000/ SHOW_ETHICAL_AND_SHARIA_FILTER=true SHOW_LANGUAGES_FILTER=true
