<li class="result t-firm">
  <%= heading_tag(level: 2, class: 'result__heading') do %>
    <div class="l-2col">
      <div class="result__firm-name">
        <span class="result__heading-text t-name">
          <%= link_to firm.name, firm_profile_link(firm.id, params[:search_form][:postcode]) %>
        </span>
      </div>
      <div class="result__profile-link is-hidden-on-mobile">
        <%= link_to t('search.result.view_profile'), firm_profile_link(firm.id, params[:search_form][:postcode]),
            class: 'outline-button outline-button--light outline-button--margin-collapse t-view-profile' %>
      </div>
    </div>
  <% end %>

  <div class="result__information">
    <% if search_form.face_to_face? %>
      <%= render partial: 'search/partials/firm/adviser_distance', locals: { distance: firm.closest_adviser_distance } %>
      <%= render partial: 'search/partials/firm/num_offices_advisers', object: firm, as: :firm %>
    <% end %>
    <div class="result__offerings-qualifications">
      <%= render partial: 'search/partials/firm/offerings', object: firm, as: :firm %>
      <%= render partial: 'search/partials/firm/qualifications', object: firm, as: :firm %>
    </div>
  </div>
</li>
